# Changelog

## [Unreleased]

Systematic behavior-preserving cleanup pass in progress on branch `cleanup/2026-08` (plan and audit trail: `docs/cleanup-2026-08-plan.md`). Entries are appended per commit and record files touched and how the change was verified.

### Changed
- Packaging + repo plumbing: dropped `trl>=0.8.0` and `datasets>=2.18.0` from `pyproject.toml`'s `[qat]` extra (neither is imported anywhere — `run_qat` hand-rolls its own collator/`Trainer`/dataset-as-list, never `trl.SFTTrainer` or `datasets.Dataset`) and, as a dependency-declaration fix rather than a pure removal, added `accelerate>=1.1.0` explicitly — `transformers.TrainingArguments` hard-requires it but `transformers` itself never declares it, so `trl` was the only thing holding the floor and dropping it without adding `accelerate` would let a fresh `pip install -e ".[qat]"` legally resolve an `accelerate` too old for `TrainingArguments` to construct; corrected the matching stale `trl/datasets` dependency-list mentions in `train.py`'s module docstring, `CLAUDE.md` (both `[qat]`-extra mentions), and `docs/qat.md`. Also: anchored `.gitignore`'s new `/o/` entry (unanchored `o/` would match at any depth) and added `.ruff_cache/`/`.venv-qat/`, both present-and-unignored at repo root (`.venv/` doesn't glob `.venv-qat`); added `.ruff_cache/` to the `Makefile` `clean` target's `rm -rf` list (deliberately not `output/` — 13 GB of irreplaceable measured validation artifacts — or `o/` — an empty, provenance-less stray dir); removed the `Makefile` `build` target entirely (invoked an undeclared `build` package, shadowed by a stale `build/` setuptools dir besides, and had no wheel/release workflow anywhere in git history), dropping it from `.PHONY` too. (2026-08-08; files: pyproject.toml, magicquant/qat/train.py, CLAUDE.md, docs/qat.md, .gitignore, Makefile; verified: full suite 901 passed/20 skipped unchanged before and after; `git check-ignore o .ruff_cache .venv-qat` all exit 0; `make -n clean` lists `.ruff_cache/`; `make -n build` now fails with "Nothing to be done"; `grep -n 'trl\|datasets' pyproject.toml` empty in the qat extra; repo-wide `trl`/`datasets` grep shows no remaining stale dependency claims, only unrelated prose in the calibration corpus and a `huggingface.co/datasets/...` URL string)
- Comment/docstring accuracy fixes in the quant-core encoder path and the QAT validation harness: reworded `schemes.py`'s IQ4_NL header comment to state its "lower noise than Q4_K_M" claim is imatrix-conditional (forward-referencing the `IMATRIX_DEPENDENT_SCHEME_NAMES` section, whose own cross-reference back was reworded in sync now that the definition site carries the qualifier); corrected the stale imatrix-scope claim in `converters.py`'s and `ggml_binding.py`'s public-API docstrings and in CLAUDE.md's Known Limitations section from "K-quants (Q2_K–Q6_K) and IQ4_NL" to the registry-grounded "K-quants and the IQ family; ignored by MXFP4/ROCmFPX/float/legacy Q8_0", documented `n_per_row` as conditionally required (required with `imatrix` only when `weights.ndim < 2` in `converters.py`; required whenever `imatrix` is not `None` in `ggml_binding.py`), and dropped a stale "PR4" forward-reference; shrank `ggml_binding.py`'s `_verify_type_ids` docstring to a short scoping note pointing at `ggml_facts.py` (which keeps the full Q8_1 upstream-bug narrative unchanged); added one-line notes marking `_LibggmlHandle.supports()` and `qat/validate.py`'s `compare_perplexity` as intentional, currently-callerless public surface (external ROCmFPX fork tooling and a hand-run validation harness respectively) rather than dead code. Comment/docstring only, zero executable-line changes. (2026-08-08; files: magicquant/quant/schemes.py, magicquant/quant/converters.py, magicquant/quant/ggml_binding.py, magicquant/qat/validate.py, CLAUDE.md; verified: full suite 901 passed/20 skipped, identical before and after)
- Duplication folds in the quant-core encoder/calibration path, plus a new drift guard: (1) `ggml_facts.py` gained one public `expected_size(name, n_elements) -> int` — the canonical ceil-div-over-block-tables body previously hand-duplicated in `converters.ggml_tensor_data_size` and `ggml_binding._expected_size`; both are now one-line delegates, keeping their names/signatures/monkeypatch-by-name behavior unchanged (`tests/test_security_bounds.py` still patches `ggml_binding._expected_size` directly) and reproducing the `.get(name, 1)`/`.get(name, 2)` fallback defaults exactly. `converters.GGML_BLOCK_SIZE`/`GGML_TYPE_SIZE` and `ggml_binding._GGML_BLOCK_SIZE`/`_GGML_TYPE_SIZE` stay as `dict(ggml_facts.*)` re-exports (still read directly by `_cross_check_block_type_sizes`/`_verify_type_ids` and asserted equal by `tests/test_block_tables.py`) but gained a comment flagging they no longer feed the size arithmetic, so a future overlay belongs in `ggml_facts` only. (2) Added `_LibggmlHandle._cross_check_requires_imatrix`, called from `__init__` alongside the existing type-id/block-size guards: cross-checks every registered scheme's static `requires_imatrix` field (`schemes.py`) against this handle's live `requires_imatrix()` answer, per scheme (not per ggml type, since several schemes share a `ggml_type_name`). Deliberately LOG-ONLY (`_cross_check_block_type_sizes`'s pattern, not `_verify_type_ids`'s hard-raise) — a mismatch can't corrupt output bytes since `writer.py`'s hard gate already reads the live value, so the only risk is an over/under-inclusive search pool, not worth killing all encoding over; safe for ROCmFPX fork types too, since `requires_imatrix()` already short-circuits to `False` for a fork type this libggml doesn't support. (3) `calibration.py`'s `_load`/`_load_source` had an identical tolerant-JSON-read body (try/`json.loads`/isinstance-dict-guard/`except (OSError, ValueError)`); extracted into a private `_read_json_dict_tolerant(path)`, called from both — kept `_load`/`_load_source` as distinct top-level callables with their two caches (`_cache`, `_source_cache`) deliberately still separate per the module docstring's don't-clobber-the-default-cache rationale, and `_load` still resolves `_CALIBRATION_PATH` as a module global at call time (required by the ~10 tests that monkeypatch it). (2026-08-08; files: magicquant/quant/ggml_facts.py, magicquant/quant/converters.py, magicquant/quant/ggml_binding.py, magicquant/quant/calibration.py, tests/test_ggml_binding_cross_check.py; verified: full suite 902 passed/20 skipped [901 baseline + 1 new test, `test_requires_imatrix_cross_check_logs_on_static_live_mismatch`]; exhaustive equivalence check of `expected_size` vs the pre-change arithmetic over all 37 registered type names + 1 unregistered name × 9 element counts, 342/342 identical; `tests/test_block_tables.py tests/test_ggml_binding_cross_check.py tests/test_calibration_loader.py tests/test_noise_calibration.py tests/test_quantization_guards.py` 57 passed; `tests/test_writer.py tests/test_iq_schemes.py tests/test_writer_imatrix.py tests/test_refactor_regression.py tests/test_imatrix_noise_calibration.py tests/test_fit_noise_factors.py tests/test_moe_stacking.py tests/test_imatrix_encode.py tests/test_rocmfpx_schemes.py tests/integration/test_encoder_parity.py` 106 passed; grep confirms the ceil-div block-arithmetic line and the tolerant-JSON `except (OSError, ValueError)` block each now appear exactly once in the repo)
- Two more duplication folds in `writer.py`'s `create_hybrid_gguf`: (1) `_ftype_map` (the `general.file_type` metadata table) is now built from `gguf.constants.LlamaFileType` by enum MEMBER NAME instead of 16 hand-typed int literals — same "facts come from upstream, never drift" policy already used for this file's `_GGUF_TYPE_*`/`GGML_TYPE` tables, and closing the exact class of bug that already bit this table once (`Q4_K->12`/`Q5_K->16`/`IQ4_NL->20` were silently wrong pre-2026-06). The 16-entry key set (a deliberate two-namespace union of ggml type names and scheme aliases, consumed both as a membership filter and as a second-keyed fallback lookup a few lines below) is preserved verbatim — every one of today's ints was confirmed to equal `int(LlamaFileType.<member>)` for its mapped member name, with no drift found, so no keys were added, removed, or renumbered. (2) Pass 1 now captures each tensor's PRE-compat desired ggml type name (`entry["_desired_ggml_name"]`, computed once alongside `target_ggml_name` before the five compat mutations — SSM-F32 force, 1D-F32, BF16->F16/Q8_0, block-32 fallback, and the can_decode passthrough overwrite) instead of leaving the bad_tensors pre-quantized-source gate to re-derive `tensor_overrides`/`group_schemes`/`base_quant` resolution from scratch 220 lines later — a second, independent copy the code's own comment already flagged as a hand-sync hazard. The gate's raise condition and both raised `ValueError` messages (including the `MAGICQUANT_ALLOW_DEQUANT_SOURCE` text) are unchanged; the entry's POST-compat `_target_ggml_name` was deliberately left unused for this purpose, since on the not-can_decode path it has already been overwritten to the source type and would make the gate's comparison always false. (2026-08-08; files: magicquant/gguf/writer.py; verified: full suite 902 passed/20 skipped, identical before and after; `tests/test_writer.py tests/test_writer_parallel_encode.py tests/test_writer_tensor_overrides.py tests/test_writer_block_fallback.py tests/test_writer_imatrix.py tests/test_writer_ssm_f32_force.py tests/test_writer_gguf_constants.py tests/test_dequant_source.py tests/test_bf16_downgrade_guard.py` 95 passed; independently re-derived the enum-backed `_ftype_map` from `gguf.constants.LlamaFileType` in a standalone one-liner and diffed it against the pre-change 16-entry literal dict — identical keys, identical values, `old == new` is `True`)
- README/docs refresh + archive moves: `git mv`'d the four superseded audit docs (`AUDIT_REPORT.md`, `LOGIC_AUDIT.md`, `AUDIT_2026-06-09.md`, `AUDIT_FIXPLAN_2026-06-09.md`) and `WORKLOG.md` into a new `docs/history/` (all five cross-reference each other by bare filename only, so no in-file link text needed to change; `AUDIT_2026-07-01.md` and `docs/superpowers/` stay put); `docs/history/WORKLOG.md` gained a one-line "session closed, see git log" note at the top rather than a blanket COMPLETE stamp, since its Round-2 section honestly still lists two GPU cells that were never measured. README.md: fixed the stale `converters.py` architecture line (numpy-block-encoder description → `encode_to_ggml_bytes()` dispatch, keeping the "single source of truth" phrase since it mirrors a live CLAUDE.md invariant); regenerated the architecture tree from the actual `magicquant/` layout (18 → 40 modules, including `v2/` framed as the non-default budget-search path and the previously-missing `S`/`N`/`V` tensor groups); expanded the scheme table with the two genuinely-missing default-pool schemes (`Q3_K`, `Q2_K`) and added an explicit opt-in/fork-only/imatrix-required breakdown for the other 14 registry schemes instead of implying all 23 are uniformly "Supported"; added Usage blocks for the three previously-undocumented CLI subcommands (`imatrix`, `qat-merge`, `card`); relabeled `make lint` from "Syntax check" to "Ruff lint (magicquant/ + tests/)". docs/qat.md: documented the `magicquant qat-merge` CLI (flags copied verbatim from `__main__.py`'s argparse wiring) and reworded the "Merge the adapters" step to point at it, disambiguating it from the unrelated in-memory `wrap.merge_qat_adapters` used by the doc's *Eval and handoff* / *Production-path proof* / *Honest caveats* sections, which are left referring to the in-memory function as before. (2026-08-08; files: README.md, docs/qat.md, docs/history/WORKLOG.md, docs/history/AUDIT_REPORT.md, docs/history/LOGIC_AUDIT.md, docs/history/AUDIT_2026-06-09.md, docs/history/AUDIT_FIXPLAN_2026-06-09.md; verified: full suite 901 passed/20 skipped, unchanged before and after; stale-reference grep for the five moved files' old root paths returns only self-references between the five now-co-located files plus one out-of-scope mention in docs/cleanup-2026-08-plan.md)
- Two more duplication folds in `magicquant/gguf/source.py`'s HF-config/tokenizer read path: (1) extracted a module-level `_resolve_effective_config(config)` for the four-times-copy-pasted "resolve the nested LLM sub-config" idiom (sub-config keys win over top-level on conflict, checked in `text_config`/`language_config`/`llm_config` order with first-match-`break` — all four sites were verified byte-for-byte identical before extraction, so the shared helper reproduces every one of them exactly) and called it from `_build_gguf_metadata_from_config`, `_build_tokenizer_metadata`'s vocab-padding block, and both of `SafetensorsSource._ensure_loaded`'s QK-permute and qwen3.5 linear-attention config-setup blocks. Also threaded an optional `config: Optional[Dict[str, Any]] = None` parameter into `_build_tokenizer_metadata`, kept optional because 7 existing test callers invoke it positionally with only `model_dir`; when omitted it re-reads config.json from disk exactly as before (byte-identical), and `_ensure_loaded`'s one production call site now passes its already-parsed `config` dict instead, eliminating that redundant re-read (`config={}` for a missing config.json resolves through the identical empty-effective-config path as today's file-absent skip, so output is unchanged either way). (2) The three BF16/F16/F32 raw-bytes-to-float32 decode sites now share the existing `_decode_st_bytes_to_f32` helper instead of each re-implementing the same three-branch bit-shift logic: `GGUFSource.read_tensor_f32` delegates only when `type_name in _NATIVE_FLOAT_TYPES`, so its fall-through to the dequantize-then-requantize path for quantized/I8/I16/I32/I64/F64 GGML types is untouched byte-for-byte; `LoRAMergedSource._read_adapter_tensor` behavior on exotic adapter dtypes changes as follows: dtypes the shared helper's table covers (I8/I16/I32/I64/F64) now numerically convert to float32 (previously: garbage float32 bit-reinterpret for I32, an accidental numpy reshape `ValueError` for the rest), and dtypes outside the table (U8/BOOL/F8, etc.) raise a deliberate `ValueError` — real PEFT checkpoints are always BF16/F16/F32, so no shipping adapter is affected, and the BF16/F16/F32 output arrays are byte-identical. (2026-08-08; files: magicquant/gguf/source.py; verified: full suite 902 passed/20 skipped, identical before and after; `tests/test_dequant_source.py tests/test_security_bounds.py tests/test_moe_stacking.py tests/test_source_qk_permute.py tests/test_lora_merged_source_peft_differential.py tests/test_source_qwen35_transforms.py tests/test_source_rope_mapping.py tests/test_source_arch_gate.py tests/test_source_bias_mapping.py tests/test_source_merges_format.py tests/test_unigram_vocab.py tests/test_chat_template_fallback.py` 99 passed/1 skipped, unchanged)
- Two duplication folds in the orchestrator's persisted-measurement plumbing: (1) `MagicQuantOrchestrator._save_results`'s "measurements" dict entry and `_write_measured_checkpoint`'s "measured" dict entry — independently hand-listing the same 11-12 fields off `self._measured.items()`, a drift the code's own BLOCKER comment records as having already shipped a real bug — now both call one new `_serialize_measurement(v, *, include_path)` staticmethod. The two sites' key ORDERS differ today (checkpoint inserts `path` after `residual` and ends `...incumbent, measurement_invalid, corpus_path`; `_save_results` has no `path` and ends `...incumbent, corpus_path, measurement_invalid`) and the helper reproduces each site's order exactly via the `include_path` branch, so on-disk JSON key order — not just content — is unchanged in both `search_results.json` and the resume checkpoint; both call-site comments (the CORPUS PROVENANCE note and the BLOCKER note, which names its guard test) were moved onto the helper verbatim. `tiered_survivors`/`tiered` stay separate keys (unchanged; already derived from one `_tiered_full` by an earlier commit) since external consumers read them with opposite precedence. (2) The `"|".join(f"{g}:{config[g]}" for g in sorted(config))` canonical `group:scheme` key one-liner — previously hand-reimplemented identically in `orchestrator._config_key`, `pareto._scheme_str`, and `evolution/predictor._make_config_key` — now lives once as `magicquant.utils.naming.config_key(config)`; all three call sites are unchanged in name/signature and now delegate to it (`pareto._scheme_str` keeps its own entry-taking signature and `entry.get("config") or {}` None-guard, since it's a display wrapper, not the contract itself). Pure code motion: this format is a PERSISTED interchange format (search_results.json / checkpoint measurement keys, parsed back by `tools/reselect_tiers.py`'s `_parse_key`), so the tests that independently hard-code the literal format (`tests/test_pareto.py`, `tests/test_reselect_tiers.py`) were deliberately left as-is rather than pointed at the new helper. (2026-08-08; files: magicquant/orchestrator.py, magicquant/pareto.py, magicquant/evolution/predictor.py, magicquant/utils/naming.py; verified: full suite 903 passed/20 skipped (902 + 1 new round-trip contract test in tests/test_naming.py); `tests/test_orchestrator_measurement.py tests/test_measured_search_checkpoint_resume.py tests/test_checkpoint_baseline_only.py tests/test_checkpoint_version_quarantine.py tests/test_pareto.py tests/test_predictor_paramcounts.py tests/test_measurement_validity.py tests/test_refactor_regression.py` 102 passed; standalone script built both HEAD (`git show`) dict-literal bodies and the new `_serialize_measurement` output for both sites across 3 representative measurement dicts (fully populated, all-optional-fields-absent, `measurement_invalid=True`) and asserted `json.dumps()` equality — key-order-sensitive — 6/6 identical)
- `cmd_analyze`'s inline `group_labels` table (9 hand-typed group-code -> display-name entries, byte-identical to `naming.py`'s `GROUP_CODES`) now delegates to `naming.py`'s previously-uncalled `get_group_names()`, with the one entry `cmd_analyze` needs beyond the shared 9 (`'UNKNOWN': 'Unclassified'`) layered on top of the returned copy; import stays function-local inside `cmd_analyze`, matching the file's existing lazy-import convention (the same pattern already used for `generate_name`). (2026-08-08; files: magicquant/__main__.py; verified: full suite 903 passed/20 skipped unchanged before and after; label-set equality confirmed by a throwaway script building both the old inline dict and `get_group_names()` + `UNKNOWN` and comparing — identical keys, values, and insertion order; `magicquant analyze --help` and a real `magicquant analyze` run against an existing GGUF fixture both produced unchanged output)
- Promoted `PredictiveScorer`'s collapse-penalty constants — previously hand-copied into `tools/fit_noise_factors.py` with only a comment ("matches PredictiveScorer's default") enforcing the link — to a single home in `magicquant/evolution/predictor.py`: module-level `DEFAULT_COLLAPSE_PENALTY_BETA = 0.02` (now used to initialize `self.collapse_penalty_beta` in `__init__`) and class attribute `PredictiveScorer.HIGH_SENSITIVITY_GROUPS = ('E', 'H', 'O', 'R')` (now referenced in `predict_loss` instead of a local list literal). `tools/fit_noise_factors.py` now imports both — placed after its `sys.path.insert(0, repo_root)` with `# noqa: E402` so standalone `python tools/fit_noise_factors.py` invocation keeps working — turning the "matches PredictiveScorer's default" comment into an enforced fact. `evolution/survival.py`'s separate `_HIGH_SENSITIVITY = {'E','H','O','R'}` set (a third copy, used for "brain" vs "attention" sampling-category bucketing and floor-clamp logic that CLAUDE.md names as a seed-pinned-fixture regeneration trigger) is deliberately left unrestructured for this change, with a new comment cross-referencing `PredictiveScorer.HIGH_SENSITIVITY_GROUPS` as the canonical home; full unification is logged future work. (2026-08-08; files: magicquant/evolution/predictor.py, magicquant/evolution/survival.py, tools/fit_noise_factors.py; verified: full suite 903 passed/20 skipped unchanged before and after; `tests/test_fit_noise_factors.py tests/test_noise_calibration.py tests/test_predictor_paramcounts.py tests/test_refactor_regression.py` 25 passed, seed-pinned fixture untouched; post-move constant values confirmed identical (`('E','H','O','R')`, `0.02`) and `PredictiveScorer` confirmed to remain a plain `__init__`-based class (no dataclass semantics affected by the new class attribute); standalone `python tools/fit_noise_factors.py --help` confirmed the deferred-import ordering still works outside the package)
- Two duplication folds in `magicquant/utils/llamacpp.py`'s tool-discovery and subprocess-measurement paths: (1) extracted module-level `_find_tool_in_dirs(possible_names, search_dirs) -> Optional[str]` for the identical nested `for d in search_dirs: for name in possible_names` loop previously copy-pasted in `_find_quantize_tool`/`_find_perplexity_tool`; both keep their own `possible_names`/`search_dirs` construction, `(self) -> str` signature, and verbatim `FileNotFoundError` messages, only raising when the helper returns None. The module-level `_find_bench_tool` folds in too — its single-directory loop is the same dirs-outer/names-inner search with a one-element `search_dirs`, verified line-by-line before folding — keeping its distinct `which`-based PATH fallback and `(perplexity_tool_path) -> Optional[str]` signature (both patched by name in tests) untouched. The dirs-outer/names-inner nesting order and `.exists()` (not `.is_file()`) are preserved exactly, since either change would silently alter which binary wins when both a legacy root binary and a modern build/bin one exist. (2) Extracted `_effective_chunks(chunks) -> str` for the `--chunks` fallback ternary byte-identical between `save_base_logits` and `calculate_kl_divergence` (same `getattr(self, "ppl_chunks", None) or -1` degrade-on-bare-`__new__` behavior), and `_run_subprocess_or_none(cmd, timeout, label)` for the four copy-pasted try/except/print/return-None triage blocks around `_run_perplexity_subprocess` in `calculate_perplexity`/`bench`/`save_base_logits`/`calculate_kl_divergence` — catches exactly `subprocess.CalledProcessError`/`subprocess.TimeoutExpired`, so a missing/wrong-arch binary's `OSError`/`FileNotFoundError` still propagates out uncaught. `calculate_perplexity`'s `try` block, which used to also wrap the output-parsing and verbose print, is narrowed to just the subprocess call (neither parse nor print can raise the two caught exceptions); `save_base_logits`/`calculate_kl_divergence` explicitly check the helper's None return before touching `result.stdout`/`result.stderr`. All four printed failure messages and every method's return-on-failure value are unchanged. (2026-08-08; files: magicquant/utils/llamacpp.py; verified: full suite 903 passed/20 skipped, identical before and after; `tests/test_llamacpp_measure.py tests/test_orchestrator_measurement.py tests/test_stream_aware.py` 92 passed/2 skipped, including `test_orchestrator_measurement.py::test_measured_search_survives_kl_and_bench_raising_oserror` confirming OSError still escapes the new subprocess helper uncaught)
- Two more v2 budget-search hygiene folds: (1) `calibrate.py`'s ~60-line copy-pasted build/measure/retry/cleanup blocks (the base-aggressive probe vs. each per-group probe inside `run_group_probes`) now share one private `_measure_probe(...)` helper covering only the build+measure+retry-loop+`finally`-unlink core; the base-aggressive-vs-per-group asymmetry — raise `ProbeMeasurementError` immediately unless `allow_partial` vs. record the outcome and defer to the end-of-function aggregate `failed` check — is semantic, not stylistic, and stays entirely at the two call sites, which pass a precomputed `quant_config` in and get a `MeasurementOutcome` back with no say in whether to raise. `run_group_probes`'s function-local `create_hybrid_gguf` import (relied on by `test_probe_config_shapes_per_mode`'s monkeypatch of `magicquant.gguf.writer.create_hybrid_gguf`) is preserved by threading the already-imported callable into the helper as a parameter rather than re-importing inside it. Each call site's distinct log message ("base-aggressive probe failed (attempt %d/%d): %s" vs. "probe build/measure failed for group %s (attempt %d/%d): %s") and `MeasurementOutcome` meta kwargs (`kind="base-aggressive"` vs. `group=`/`probe_scheme=` — the latter deliberately carries no `kind` key) are reproduced exactly via a log-template/prefix-args/meta parameterization, not merged into one shared string. `_probe_config(group)` (a pure, non-raising dict builder) now evaluates just outside the retry loop instead of inside its `try`, a documented no-op semantic shift. Four new characterization tests close the previously-zero coverage on these failure paths: retry-then-succeed (`attempts == 2`, probe GGUF unlinked on both attempts), retries-exhausted-with-`--allow-partial-probes` recording a failure instead of raising, a per-group failure without `allow_partial` raising `ProbeMeasurementError` via the aggregate check, and a base-aggressive failure writing its `__base_aggressive__` failure entry to `v2_probes.json` *before* raising. (2) `interchange.py` gained `log = get_logger(__name__)` matching its siblings, and its corrupt-`search_results.json` diagnostic switched from a bare `print(..., flush=True)` to `log.warning(...)` with identical message text and control flow (the `data = {}` treat-corrupt-as-absent recovery is untouched) — this removes the last bare `print()` in `magicquant/v2/`. One new test exercises the previously-uncovered corrupt-file branch, characterizing — not changing — that recovery, including its interaction with the legacy-file-must-not-be-stamped rule (a corrupt file is indistinguishable from absent, so it takes the `fresh` path and does get `tier_scheme_version` stamped). (2026-08-08; files: magicquant/v2/calibrate.py, magicquant/v2/interchange.py, tests/test_v2_calibrate.py, tests/test_v2_interchange.py; verified: full suite 908 passed/20 skipped [903 baseline + 5 new tests: 4 in test_v2_calibrate.py, 1 in test_v2_interchange.py], identical otherwise before and after; `tests/test_v2_calibrate.py tests/test_v2_interchange.py tests/test_v2_sensitivity.py tests/test_v2_allocate.py tests/test_v2_resolve.py tests/test_probe_errors.py` 36 passed)
- Decomposed `_LibggmlHandle.encode()` (`ggml_binding.py`) into a short orchestrator plus two private helpers: `_resolve_quantize_shape(flat_size, imatrix, n_per_row)` extracts the imatrix-shape-derivation block, raising the identical `ValueError`s with identical messages, and reproduces the contractual n_per_row override verbatim (when `imatrix` is `None`, any caller-supplied `n_per_row` is discarded in favor of `flat_size` with `nrows=1`, per `encode()`'s own docstring); `_quantize_chunk_or_raise(...)` extracts the duplicated "call `ggml_quantize_chunk`, raise `RuntimeError` on a byte-count mismatch" block shared by the single-chunk dispatch and the per-expert MoE loop, taking `context`/`detail` params so both original message shapes (` for expert slice N/M` vs. `, n_elements=N`) are reproduced exactly rather than merged. `encode()`'s validation, output-size bound check, and the per-expert offset math/pointer-lifetime handling are untouched; `_expected_size` stays a bare module-global lookup at both call sites (`tests/test_security_bounds.py` still monkeypatches it by name). Both helpers are called with pointers the caller builds from named locals, preserving ctypes buffer lifetime. (2026-08-08; files: magicquant/quant/ggml_binding.py; verified: full suite 908 passed/20 skipped, identical before and after; `tests/test_quantization_guards.py tests/test_imatrix_encode.py tests/test_ggml_binding_cross_check.py tests/test_ggml_decode.py tests/test_security_bounds.py tests/test_block_tables.py tests/integration/test_encoder_parity.py` 63 passed, `test_encoder_parity.py` confirmed to run, not skip [9 passed]; standalone byte-identity script loaded the pre-change HEAD version of `ggml_binding.py` and the post-change version as two independent modules and compared `_LibggmlHandle.encode()` output across Q8_0/Q4_K/Q6_K/MXFP4 x {plain 2-D, imatrix+n_per_row 2-D, imatrix on 1-D flat+n_per_row, non-block-aligned n_per_row with imatrix=None (the override case), per-expert imatrix with n_slices=2} plus all six extracted `ValueError` paths and both `RuntimeError` paths (forced via a fake `ggml_quantize_chunk`) — 30/30 identical, 0 mismatches)
- Decomposed two more god-functions in `magicquant/gguf/source.py`: (1) `SafetensorsSource._ensure_loaded` (~260 lines, 10 jobs) into a slim orchestrator calling seven private helpers in the original order — `_discover_files`, `_load_config_and_gate_arch` (returns `(config, arch)`), `_resolve_qk_permute_cfg`, `_resolve_qwen35_cfg`, `_parse_headers_and_map_names` (per-file header parse + MoE-expert grouping + name mapping, with the unmapped-name gate folded in at its tail since it consumes that loop's `unmapped_names` and must run after all files are parsed but before stacking), `_stack_moe_experts`, and `_alias_tied_embeddings` — with `self._loaded = True` left at the very top of `_ensure_loaded`, before any of the seven calls, exactly as before (now with a comment explaining why: a gate raising mid-load must leave the object flagged loaded with partial state so a retry returns silently rather than re-raising, which `LoRAMergedSource.__init__`'s bare `except Exception` around `get_metadata()` depends on). (2) `_build_tokenizer_metadata` (~230 lines) into `_extract_tokenizer_vocab` (tokenizer.ggml.model/pre + vocab/tokens/scores/token_type + merges, returning `(meta, vocab, added)` for the next stage), `_resolve_special_tokens` (bos/eos/pad/unk id resolution + add_bos_token/add_eos_token from tokenizer_config.json), and `_resolve_chat_template` (tokenizer_config.json chat_template, then the chat_template.jinja/.json file fallbacks, then the nothing-emitted warning); tokenizer_config.json is now read once in the slim orchestrator and the parsed dict — or a new module-level `_NO_TOKENIZER_CFG` sentinel when the file is absent, so a file parsing to literal JSON `null` still crashes exactly as before — threaded to both of the latter two helpers; one dead loop binding revealed by the split was renamed to `_hf_name`, instead of the special-token block alone gating that read as before (same content, same read count). The bos_token_id double-write's priority — config.json's value (`_extract_tokenizer_vocab`) written first, tokenizer_config.json's `bos_token` (`_resolve_special_tokens`) conditionally overwriting it only when the token string resolves to a known id — is unchanged; a new comment at the second write states this explicitly, mirroring the existing qwen35 rope_theta-takes-priority-over-field_map comment convention. One accepted, disclosed edge-case behavior delta (flagged in code at the `added` binding in `_extract_tokenizer_vocab`, too): `_build_tokenizer_metadata` has a pre-existing latent `UnboundLocalError` (not touched or fixed here — the added_tokens list is only bound inside the vocab function's `if vocab:` block, same as before), and decomposition widened one of its two sub-cases. (empty vocab, tokenizer_config.json PRESENT): crashed on HEAD and still crashes now — unchanged. (empty vocab, tokenizer_config.json ABSENT): HEAD succeeded (the `for at in added:` reference lived inside the same `if os.path.exists(config_path):` block that guarded tokenizer_config.json's presence, so it was never reached); the new code raises, because `added` must cross `_extract_tokenizer_vocab`'s return boundary to reach `_resolve_special_tokens` regardless of whether tokenizer_config.json exists. No code path in the test suite or any known real tokenizer.json exercises either sub-case (real BPE/Unigram vocabs are never empty). (2026-08-08; files: magicquant/gguf/source.py; verified: full suite 910 passed/20 skipped, identical before and after; `tests/test_dequant_source.py tests/test_security_bounds.py tests/test_moe_stacking.py tests/test_source_qk_permute.py tests/test_source_qwen35_transforms.py tests/test_source_rope_mapping.py tests/test_source_arch_gate.py tests/test_source_bias_mapping.py tests/test_source_merges_format.py tests/test_unigram_vocab.py tests/test_chat_template_fallback.py tests/test_lora_merged_source_peft_differential.py` 99 passed/1 skipped, unchanged; standalone byte-identity script loaded the pre-change HEAD `source.py` (git worktree) and the post-change version as two independently-named modules and compared, across a qwen35moe fixture (text_config-nested config, a linear-attention layer, MoE experts split across two shards discovered via `model.safetensors.index.json`, tied embeddings, a BF16 tensor, config.json vocab_size padding, and a tokenizer_config.json bos_token that overrides config.json's) and a llama fixture (dense QK-permute path, no tokenizer_config.json, a chat_template.jinja fallback): `get_metadata()` including key order, `get_tensor_names()` order, `get_all_tensors_info()`, `read_tensor_f32()` bytes for every tensor including the stacked MoE experts and the tied `output.weight` alias, and captured log warnings — all identical; separately confirmed the arch-gate `UnsupportedSourceArchitecture` raise and the unmapped-tensor-name raise produce byte-identical exception type and message text on both versions)
- Decomposed `writer.py`'s ~570-line `GGUFWriter.create_hybrid_gguf` into seven private helper methods, called in the original order with all local state passed explicitly: `_validate_overrides` (the up-front `tensor_overrides` scheme-name check), `_warn_unmatched_overrides` (the typo'd-tensor-name warning), `_prescan_unknown_tensors` (the cosmetic group-classification warning, kept distinct from the hard-error undecodable-source gate below), `_resolve_pass1_entries` (the Pass-1 per-tensor loop — moved as a single unbroken unit specifically so the five-stage type-mutation pipeline it runs in strict order — SSM-conv-F32 force, 1D-F32 force, BF16->F16/Q8_0, block-32 K-quant fallback, and the terminal `can_decode` passthrough overwrite — cannot be reordered across an extraction boundary), `_validate_tensor_entries` (the three post-hoc gates: undecodable/pre-quantized-source, double-quantization warning, imatrix-required), `_build_metadata` (metadata dict construction including `general.file_type`; still WIPES any caller-set `self.metadata` exactly as before — existing behavior, unchanged), and `_publish` (write + `.partial`/`os.replace` + grouped fallback-summary log). Every helper's body is a verbatim move, dedented, with only signature/return-value plumbing added — no block was reordered, split mid-body, or had its logic altered; `self._fallbacks`'s reset-per-call timing, `self._bf16_downgrade_warned`'s once-per-writer-lifetime scope, and `classifier.warn_unclassified_once()`'s post-Pass-1 firing point are all unchanged. Also hoisted `_ftype_map` (the `general.file_type` lookup table, enum-derived per the duplication-fold entry above) from a per-call function-local dict to a module-level constant next to `GGML_TYPE`/`_GGUF_TYPE_*` — built once at import instead of once per `create_hybrid_gguf` call, so a future upstream `LlamaFileType` rename now surfaces as an `AttributeError` at import time rather than mid-build; added a matching drift-tripwire test pair to `tests/test_writer_gguf_constants.py` (`test_ftype_map_matches_frozen_snapshot`, pasting the 16 historical name->int pairs verbatim, and `test_ftype_map_derived_from_installed_gguf_package`, confirming the map agrees with the installed package's enum member-by-member — distinguishing upstream-renumbered from our-map-drifted when either test fails), following that file's existing two-test pattern for `_GGUF_TYPE_*`/`ALIGNMENT`. (2026-08-08; files: magicquant/gguf/writer.py, tests/test_writer_gguf_constants.py; verified: full suite 910 passed/20 skipped [908 baseline + 2 new tests], identical otherwise before and after; `tests/test_writer.py tests/test_writer_parallel_encode.py tests/test_writer_tensor_overrides.py tests/test_writer_block_fallback.py tests/test_writer_imatrix.py tests/test_writer_ssm_f32_force.py tests/test_writer_gguf_constants.py tests/test_dequant_source.py tests/test_bf16_downgrade_guard.py tests/test_moe_end_to_end.py tests/test_head_aggressive.py` 107 passed; standalone byte-identity script ran the pre-change HEAD `writer.py` (git worktree) and the post-change version against an identical synthetic ModelSource — 9 tensors spanning 8 groups (E/N/Q/K/O/U/D/S), an SSM `ssm_conv1d` tensor forced F32, a 1-D norm forced F32, a BF16-designated group hitting both the plain F16-downgrade warning and the out-of-range Q8_0-substitution branch (whose Q8_0 result then itself fails the block-32 check and chains into a second, F32 fallback — exercising the documented already-mutated-target-name interaction), a Q4_K_M-designated row not divisible by 256 but divisible by 32 falling back to MXFP4, and a `tensors` override — produced byte-identical output GGUFs (sha256 `82148fde635f33ee74aed89114508a7e8566136a61f9ef9447ef048e0463ac6c` both) and byte-identical captured stdout/log-warning text, differing only in the output file path string and non-deterministic MB/s timing noise)
- Two more duplication folds plus one god-method decomposition in `orchestrator.py`'s search setup: (1) `_detect_search_groups(verbose)` now holds the E/H/Q/K/O/U/D (+X/R for MoE, +S for SSM) tensor-group-detection block previously duplicated verbatim in `run_measured_search` and `run_full_search` -- re-diffed byte-identical at this tree immediately before folding, confirming it had not drifted since. Its `from magicquant.gguf.source import open_model_source` stays function-local inside the new helper (not hoisted to module scope), preserving the exact monkeypatch-by-name seam several tests depend on: `tests/test_strict_probing.py::test_measured_search_constructs_strict_prober` ("Group detection opens the source -- stub it.") and the `_make_orchestrator` fixtures in `tests/test_orchestrator_measurement.py`, `tests/test_tps_objective.py`, `tests/test_measurement_validity.py`, `tests/test_measured_search_checkpoint_resume.py`, `tests/test_incumbent_seeding_orchestrator.py`, `tests/test_noise_calibration.py`, and `tests/test_speed_aware_selection.py` all `monkeypatch.setattr(source_mod, "open_model_source", ...)` before calling into a `run_*_search` method. `_build_predictor(calibration_source, baseline_size_gb)` now holds the also-byte-identical `PredictiveScorer(...)` construction call, previously duplicated the same way; both callers still assign its return value to `self.predictor` themselves. `SensitivityProber`/`EvolutionarySurvivor` construction is deliberately left untouched -- the two paths' construction genuinely differs (`strict=True`/`parameter_counts`/KL-logit paths on the measured side only), a real divergence already logged in this changelog's "Future work" section below. (2) `run_measured_search`'s per-candidate record/checkpoint/cleanup block (KL+PPL fusion, the `eps`/`measurement_invalid` physical-plausibility guard, `self._measured` population, predictor residual feedback, checkpoint persist, candidate-GGUF unlink) is now `_record_candidate_measurement(path, config, config_key, incumbent_tier_by_key, enable_kl, enable_speed_bench, checkpoint_path, verbose)`, a pure move verified byte-identical (148/148 lines, 0 diffs) against the pre-move block by an automated line-for-line comparison; 53 of those 148 lines are `git blame`-confirmed to be the 9-day-old `a6f8dd0` "never let a non-measurement become a number" fix (2026-07-30) and are carried verbatim, comments included -- no reordering, no simplified `eps` expression, `predictor.record_residual` still gated by the same `if not measurement_invalid:`. The `if path is None: continue` precondition stays at the call site, above the new method call, exactly as before. (2026-08-08; files: magicquant/orchestrator.py; verified: full suite 910 passed/20 skipped, identical before and after; targeted set `tests/test_orchestrator_measurement.py tests/test_measured_search_checkpoint_resume.py tests/test_checkpoint_baseline_only.py tests/test_checkpoint_version_quarantine.py tests/test_measurement_validity.py tests/test_measurement_candidate_coverage.py tests/test_tps_objective.py tests/test_speed_aware_selection.py tests/test_search_groups.py tests/test_refactor_regression.py tests/test_noise_calibration.py tests/test_incumbents.py tests/test_evolution_seeding.py` 169 passed; `tests/test_strict_probing.py tests/test_incumbent_seeding_orchestrator.py` 11 passed; a throwaway script re-extracted both group-detection blocks and both `PredictiveScorer` construction call sites from this tree and diffed them against each other immediately before folding -- 0 output both times; a second script diffed the extracted `_record_candidate_measurement` method body, dedented, against HEAD's pre-move inline block line-for-line -- 148/148 identical)
- New characterization test for `magicquant/v2/search.py`'s `run_budget_search` (the v2 budget-search pipeline entry point — previously zero test coverage on the ~260-line orchestration function itself, only on the sub-functions it calls into): `tests/test_v2_search_characterization.py` adds 17 tests driving the real, unmodified function end to end through a fake `LlamaCppTools`, a monkeypatched `magicquant.gguf.writer.create_hybrid_gguf`, a stubbed `magicquant.v2.search.compute_distortion_table`, and a stubbed `magicquant.imatrix.ensure_imatrix` — extending `tests/test_v2_calibrate.py::test_probe_config_shapes_per_mode`'s existing stub pattern rather than inventing a new one. Coverage: the happy path (baseline measurement -> imatrix/scheme resolution -> distortion table -> κ calibration via real group probes -> allocation + neighbor anchors -> anchor build/verify -> results assembly), asserting the exact `v2_results.json`/`frontier.json` key sets and values, tool-call order, single-`LlamaCppTools`-instance identity, and the `measurement_chunks`/`ppl_chunks` save-before-probes/restore-after-probes invariant; all three hard `RuntimeError` paths (unmeasurable baseline, empty scheme choice set after filtering, zero verified anchors); the uncaught `BudgetInfeasibleError` path when the PRIMARY budget allocation is infeasible (contrasted with a neighbor-anchor allocation failure, which is caught and recorded instead); `failures`-list accumulation from each of its three distinct call sites (probe, anchor-allocate, anchor), including the probe-failure -> imputed-median-κ fallback; and `keep_anchors=True`'s no-unlink branch. Test-only; `run_budget_search` is unmodified by this entry. A follow-up mutation-testing pass (adversarial review, not self-assessed) found the report-fit tail (`_compute_report_fit`) was smoke-tested only — shape-checked but not value-pinned — and three mutations survived the original 14: dropping all probe points from the fit, substituting `baseline_ppl` for `probe_baseline` in the probe rel-ΔPPL formula, and inverting the `"__base_aggressive__" in probe_outcomes` cumulative-mode detection. Closed with 3 additional tests: the happy-path assertion now pins `report_fit_affine`'s exact floats (independently re-derived by running the unmodified function against the fixture, not copied from the review) rather than just checking shape, which alone catches the drop-all-probe-points and inverted-cumulative-detection mutations; a new `probe_mode="cumulative"` test exercises the previously-never-run cumulative branch of `_compute_report_fit` directly (asserting `report_fit_affine is None` with a single anchor and no leaked probe points — the inverted-detection mutation makes it leak and become non-`None`); and a new test with a stateful fake makes the `__slice_baseline__` probe measure a DIFFERENT value than the phase-1 baseline (every other fixture measures both from the same path and so can't distinguish them), pinning a `report_fit_affine` value that only matches when `probe_baseline`, not `baseline_ppl`, is used. All three mutations were hand-applied to `_compute_report_fit`, confirmed CAUGHT (test failures) with the new tests in place, then reverted with the file diffed byte-identical to pre-mutation both times. (2026-08-08; files: tests/test_v2_search_characterization.py; verified: full suite 927 passed/20 skipped [910 baseline + 17 new]; the 14 original tests confirmed green against the unmodified function before any decomposition, and all 17 confirmed green post-decomposition; all 3 previously-surviving mutations independently hand-applied and reconfirmed CAUGHT against the strengthened suite, `magicquant/v2/search.py` restored byte-identical — `sha256sum` matched pre-mutation — after each)
- Decomposed `magicquant/v2/search.py`'s ~260-line `run_budget_search` (7 inline phases, identified by its own numbered banner comments, with zero direct test coverage before the characterization test above) into seven private helpers called in the original order — `_measure_baseline`, `_resolve_imatrix_and_schemes`, `_build_distortion_table`, `_calibrate_kappa`, `_allocate_frontier_and_anchors`, `_build_and_verify_anchors`, `_compute_report_fit` — plus `_assemble_results` for the results-dict/three-file-write/completion-logging tail, reducing `run_budget_search` itself to a ~30-line coordinator. Pure code motion: every log call, exception message, and the `failures`-list accumulation order (probe, then anchor-allocate, then anchor) are reproduced verbatim. Four ordering/identity dependencies were deliberately preserved rather than "cleaned up": the single `LlamaCppTools` instance and the `cfg.measurement_chunks` -> `tools.ppl_chunks` assignment stay in the coordinator, not hoisted into `_measure_baseline`, since `_resolve_data_file`'s corpus pin (read in `_assemble_results`) and `run_group_probes`' `ppl_chunks` save/restore (`_calibrate_kappa`) both depend on one shared instance surviving every phase; `probe_outcomes`/`eps_sums` are threaded through `_calibrate_kappa`'s return and into `_compute_report_fit` intact, preserving the `"__slice_baseline__"`/`"__base_aggressive__"` sentinel-key handling and cumulative-mode detection; the primary (budget) allocation's `BudgetInfeasibleError` still propagates uncaught out of `_allocate_frontier_and_anchors`, while only the neighbor-anchor allocation loop's failures are caught and recorded; and the zero-verified-anchors `RuntimeError` is deliberately kept inside `_build_and_verify_anchors` rather than moved to the coordinator — a documented choice, not an accidental relocation relative to the results write. (2026-08-08; files: magicquant/v2/search.py; verified: full suite 927 passed/20 skipped, identical before and after [includes the 3 characterization tests added by the later mutation-testing hardening pass above, which are also unaffected by this decomposition]; `tests/test_v2_search_characterization.py tests/test_v2_calibrate.py tests/test_v2_interchange.py tests/test_v2_allocate.py tests/test_v2_sensitivity.py tests/test_v2_resolve.py tests/test_probe_errors.py` 53 passed, all 17 characterization tests unchanged and green post-decomposition; manual diff review (`git diff --stat`: 131 insertions/27 deletions, no other files touched) confirmed every moved line is byte-identical to HEAD, only rewrapped into new function signatures, return statements, and coordinator call/unpack lines)
- QAT decomposition + duplication folds (five changes, one commit): (1) decomposed `qat/train.py`'s 309-line `run_qat` into four private helpers called in the original order — `_parse_run_cfg` (a new `_RunConfig` dataclass replacing ~18 loose locals, preserving the `expert_quant_mode` fast-fail raise BEFORE the model loads), `_log_expert_cost`, `_build_training_args`, and `_build_run_meta` (the `qat_meta.json` dict, all 29 keys plus the 2 conditional `scheme_by_tensor_*` ones) — with `config_hash` still computed before the checkpoint-identity check and `base_weight_map` still loaded exactly once and threaded to both the adapter-key preflight and `_save_adapters` unchanged; the preflight block and its "raise, don't catch" contract, and the duplicate `named_modules()` QATLinear walk at wrap-time vs. preflight-time, were deliberately left inline per the audit's explicit "do not tidy" notes. (2) `expert_wrap.py`'s `expert_cache_enabled`/`expert_cache_disabled` (two 19/20-line classes differing by one boolean) collapsed onto one `@contextlib.contextmanager` factory `_expert_cache_state(enabled)`; both public names stay as plain functions returning it, both docstrings (the "definitely off" and the `CheckpointError`/gradient-checkpointing warning) preserved verbatim, and the save-prev→set→clear-on-entry / restore→clear-on-exit ordering and never-suppress-exceptions contract reproduced exactly via try/finally. (3) `wrap.py`'s `_set_submodule` now walks to the parent via `expert_wrap.py`'s already-imported `_split_param_path`/`_get_submodule` primitives instead of a second hand-rolled digit-aware traversal loop; only the final digit-vs-`setattr` write (which has no `expert_wrap` equivalent) stays local. (4) `qat/_ggml_ref.py`'s test-only `_bf16_roundtrip` now calls `converters._encode_f32_to_bf16` (the GGUF writer's real encoder) for the RNE rounding step instead of a second copy of its bit-math, then only widens the resulting bytes back to f32. (5) Added an additive `strict: bool = False` keyword to `gguf/source.py`'s `_hf_name_to_gguf` (returns `None` on no-match instead of the input unchanged, gated behind the new keyword-only param so all three existing non-strict call sites — including the `.bias` branch's own inner recursive call, deliberately kept non-strict — are untouched); `qat/names.py`'s `hf_to_ggml_name` now collapses to one `_hf_name_to_gguf(name, strict=True)` call, and its `_matches_a_pattern` second-table-scan helper (plus its inline copy of the multimodal-prefix-strip) is deleted entirely. All five are pure code motion / additive-default changes with no behavior change on any existing call path. (2026-08-08; files: magicquant/qat/train.py, magicquant/qat/expert_wrap.py, magicquant/qat/wrap.py, magicquant/qat/_ggml_ref.py, magicquant/qat/names.py, magicquant/gguf/source.py; verified: `.venv` (no torch) full suite 927 passed/20 skipped, identical before and after; `.venv-qat` full suite 3 failed/1093 passed/3 skipped identical before and after (the 3 failures are the pre-existing scipy-absent `test_probe_resolution.py::TestPredictorTracking` gap, unrelated to this change); the 14-file QAT-specific subset (`test_qat_train.py test_qat_wrap.py test_qat_expert_wrap.py test_qat_merge.py test_qat_hardening.py test_qat_key_reconciliation.py test_qat_config.py test_qat_smoke.py test_fake_quant.py test_qat_handoff.py test_qat_cli.py test_qat_checkpoint.py test_qat_diskmap.py test_qat_validate.py`) 197 passed, identical before and after; a standalone end-to-end harness ran the real `run_qat` (patched `Trainer`, real tiny `LlamaForCausalLM`, real on-disk weight-map reconciliation) through 6 scenarios — default config, a real weight-map + `scheme_by_tensor` + expert config, a matching-hash resume, a mismatched-hash resume (must raise `RuntimeError` before `trainer.train()`), an unresolvable-preflight raise (must raise `QATKeyReconciliationError` before `trainer.train()`), and the `expert_quant_mode` fast-fail (must raise before the model loads) — and diffed the full captured `qat_meta.json`, `TrainingArguments` snapshot, and exception type/message for each: byte-identical before and after; a second standalone script compared the pre- and post-refactor `_bf16_roundtrip` bit-for-bit (including the raw `uint32` view, NaN mask) across denormals/±inf/NaN/±0/RNE-tie-boundary/non-contiguous/Fortran-order/float64 inputs: 16/16 identical; a third standalone script auto-generated a concrete matching string for every regex in `_HF_TO_GGUF_PATTERNS` (via `re._parser`, not hand-transcribed) at multiple layer indices, plus their bare-module-path/`.bias`/multimodal-prefix variants and 20k seeded fuzz names, and compared old vs. new `hf_to_ggml_name` over the resulting 15,628-name corpus: 0 divergences, plus explicit spot-checks that `hf_to_ggml_name("output") == "output.weight"` (the pattern-independent self-map) and `hf_to_ggml_name("model.language_model.lm_head") is None` (the pre-existing multimodal-head gap) both still hold) One disclosed contract narrowing from the contextmanager conversion: a factory-produced context manager is single-use (HEAD's class instances were re-enterable); all 8 existing call sites construct inline, so none is affected.
- `tools/reselect_tiers.py` decomposition plus two readability-only one-liners in `magicquant/__main__.py`: (1) decomposed the 120-line, five-job `analyze()` into `_rows_from_measurements` (measurement dict -> report rows, `_parse_key` fallback preserved), `_corrected_ladder` (per-band lowest-loss winner), and `_shipped_findings` , with `analyze()` reduced to sequencing; the three untested tie-break/aliasing subtleties were carried over verbatim — `usable` is still built and handed to the two new helpers in measurements-dict insertion order (before, not after, the later `rows.sort(key=size_gb)` that only affects `report["candidates"]`'s display order), the least-squares `candidates` list is still rebuilt from every row (including implausible ones) rather than from `usable`, and the `float(shipped_loss if shipped_loss is not None else "inf") - 1e-12` sentinel is untouched. (2) `cmd_card`'s single-line `base_name = args.base_model or Path(args.model).stem if getattr(args, "model", None) else (args.base_model or "model")` (correct today, but the `or`-inside-ternary precedence isn't obvious on a read) rewritten as an explicit if/elif/else with the same `getattr(args, "model", None)` guard, identical semantics. (3) `cmd_probe`'s `baseline_ppl = args.baseline_ppl if hasattr(args, 'baseline_ppl') and args.baseline_ppl else 5.0` simplified to `args.baseline_ppl or 5.0` — only the `hasattr` half was dead (the `probe` subparser always declares `--baseline-ppl` with `default=5.0`, so the attribute is guaranteed present on any Namespace `cmd_probe` can actually receive); the truthiness half is kept intentionally, since it is what turns an explicit `--baseline-ppl 0` into the 5.0 default and prevents a downstream `ZeroDivisionError` in `evolution/probing.py`. (2026-08-08; files: tools/reselect_tiers.py, magicquant/__main__.py; verified: full suite 927 passed/20 skipped, identical before and after; `tests/test_reselect_tiers.py` 10/10 passed before and after; a throwaway AST-derived equivalence script compared old vs. new `base_name` logic across 28 (base_model x model) truthiness combinations — a superset of the required 8 — 0 mismatches; a second script compared old vs. new `baseline_ppl` logic across `--baseline-ppl` values 0, 0.0, 5.0 (argparse default), and 2.7 — the only states a real CLI dispatch can produce — 0 mismatches; the truly-attribute-absent state, which `hasattr` guards against but which argparse's fixed wiring cannot produce, was also exercised and confirmed to diverge only in that unreachable branch)

### Removed
- Dead code in GGUF reader/tensor-groups: `GGUFReader.GGUF_TYPES` (unread table) and `GGUFReader.find_tensors_by_group` (zero-caller method, plus its local `TensorGroupClassifier` import) deleted from `reader.py`; `get_model_architecture`'s unreachable tensor-name-substring fallback simplified to fall straight through to `'unknown'`; `TensorGroupClassifier.get_group_info` (zero-caller method) and the unused `Tuple` typing import deleted from `tensor_groups.py`; three `_HEURISTIC_KEYWORDS` entries removed as unreachable duplicates of Pass-1 `GROUP_PATTERNS` matches already covering the same substrings (`'layernorm'` from group N, `'router'` and `'exp_probs_b'` from group R). (2026-08-08; files: magicquant/gguf/reader.py, magicquant/gguf/tensor_groups.py; verified: full suite 903/20 unchanged)
- Dead code + import hygiene in `writer.py`: deleted the never-referenced `_GGML_TYPE_NAME` reverse table (the load-bearing `GGML_TYPE` forward table on the line above is untouched); deleted zero-caller `GGUFWriter.set_metadata`/`get_metadata` wrapper methods (the `self.metadata` dict they wrapped stays exactly as populated by `create_hybrid_gguf`); collapsed the getattr-guarded duck-typing fallbacks for `can_decode`/`read_tensor_raw` (both unreachable now that every source is a `ModelSource` subclass) into direct calls at both call sites, keeping the `raw is None` `RuntimeError` guard intact; renamed the second, unrelated `unknown_tensors` binding inside `create_hybrid_gguf`'s bad-tensors validation gate to `undecodable_source_tensors` to remove its name collision with the earlier group-classification `unknown_tensors` list; dropped the redundant `import queue as _queue_mod` in favor of the module-level `import queue`'s `queue.Empty`, and hoisted the two local `import json` statements to one module-level import. (2026-08-08; files: magicquant/gguf/writer.py; verified: full suite 903/20 unchanged before and after, plus tests/test_writer.py tests/test_writer_parallel_encode.py tests/test_writer_tensor_overrides.py tests/test_writer_block_fallback.py tests/test_writer_imatrix.py tests/test_dequant_source.py 73 passed)
- Dead-code/hygiene sweep across the orchestrator and evolutionary-search stack: deleted `orchestrator.py`'s superseded standalone `main()` / `if __name__ == "__main__"` CLI entry point (fully superseded by `magicquant/__main__.py`'s packaged CLI; zero callers repo-wide and in the one external consumer); deleted the unused `QUANT_TYPE_MAP` dict + `get_llamacpp_quant_type()` from `utils/llamacpp.py` and dropped `get_llamacpp_quant_type` from orchestrator.py's import (kept `LlamaCppTools`), removing the now-unused `Dict` typing import too; **deleted `LlamaCppTools.quantize_model` entirely** -- the llama-quantize CLI subprocess wrapper, a public-ish API surface removal, since MagicQuant quantizes exclusively via `ggml_binding`/libggml and never shells out to this wrapper -- along with its two dedicated tests in `test_llamacpp_measure.py` (`test_quantize_model_cmd_unchanged_when_threads_unset`, `test_quantize_model_cmd_appends_nthreads_positional_when_set`); `_find_quantize_tool`/`self.quantize_tool`/`_find_llamacpp`'s `which llama-quantize` discovery anchor are untouched (construction contract and `cmd_dry_run`'s log line stay byte-identical) and `_find_quantize_tool` now carries a comment explaining why the binary is still located at all; dropped unused `Tuple`/`numpy` imports from `evolution/predictor.py`; `_save_results`'s `tiered`/`tiered_survivors` per-tier dicts are now built once, with `tiered_survivors` derived from `tiered` by dropping `predicted_loss` (both keys' JSON key order and the `size_gb`/`predicted_size_gb` fallback preserved verbatim); deleted `evolution/survival.py`'s redundant local re-import of `IMATRIX_DEPENDENT_SCHEME_NAMES` inside `_walk` (the module-level import already covers it and has other unqualified users); deleted `evolution/probing.py`'s three zero-caller methods (`get_high_sensitivity_groups`, `get_probes`, `SensitivityAnalysis.identify_robust_groups`) plus its dead `__main__` demo block, keeping the live `get_normalized_weights`/`recommend_protected_groups`; deleted `evolution/survival.py`'s permanently-empty `self.history` + `get_discovered_configs` (never appended to, always returned `[]`), keeping the live `tier_winners`/`get_best_config_per_tier`; deleted `PredictiveScorer.get_sensitivity_weights`/`update_sensitivity_weights` from `evolution/predictor.py` (dead; `record_residual` immediately above is untouched); and added `self._speed_metric = "bytes"` to the orchestrator's `__init__` alongside its `_speed_aware`/`_speed_epsilon` siblings so a normally-constructed instance always has the attribute set (the `getattr` fallbacks at the read sites are unchanged, including `_speed_aware`'s deliberately-different bare-`__new__` fallback). (2026-08-08; files: magicquant/orchestrator.py, magicquant/utils/llamacpp.py, magicquant/evolution/predictor.py, magicquant/evolution/survival.py, magicquant/evolution/probing.py, tests/test_llamacpp_measure.py; verified: full suite 901 passed/20 skipped [903 baseline minus the 2 deleted quantize_model tests], plus tests/test_llamacpp_measure.py tests/test_orchestrator_measurement.py tests/test_speed_aware_selection.py tests/test_tps_objective.py 129 passed/2 skipped)
- Dead-code/simplification sweep in the v2 budget-search path: deleted `allocate.py`'s zero-reference `Unit.fixed` property (the `choices` field's `# ...fixed units have exactly 1` comment above it, which documents the unrelated live `entry["fixed"]` distortion-table dict key, is untouched); simplified `calibrate.py`'s kappa-censoring floor from `max(rel, censor_floor, MIN_REL_DPPL)` to `max(censor_floor, MIN_REL_DPPL)` (the enclosing `if rel < censor_floor:` guard makes `rel` provably unable to win the max, including for NaN since `nan < censor_floor` is `False`; `MIN_REL_DPPL` stays, since `censor_floor` can legitimately fall below it) with a comment explaining why; deleted `sensitivity.py`'s unreachable `if actual == "BF16":` distortion branch (the writer-parity `resolve_tensor_type` rule already rewrites every `BF16` target to `F16` before `compute_distortion_table` sees it, so the branch above it — `if actual == "F16":` — is the one that always fires; the separate, still-live `_FLOAT_TYPES` tuple is untouched); and hoisted `sensitivity.py`'s repeated 3-D stacked-MoE-imatrix condition (`w.ndim == 3 and m_full is not None and m_full.size == w.shape[0] * cols`) into one `stacked_moe_imatrix` boolean computed once after the imatrix-reconciliation block and reused at its one other call site, copied verbatim (no predicate changes). (2026-08-08; files: magicquant/v2/allocate.py, magicquant/v2/calibrate.py, magicquant/v2/sensitivity.py; verified: full suite 901 passed/20 skipped unchanged before and after, plus tests/test_v2_allocate.py tests/test_v2_calibrate.py tests/test_v2_sensitivity.py tests/test_v2_interchange.py tests/test_v2_resolve.py 29 passed)
- Dead-code + import-hygiene sweep in the quant-core encoder path: deleted `QuantizationScheme.is_moe_optimized` (zero readers anywhere in the codebase; three-line removal — the field declaration plus its two `is_moe_optimized=True` assignments on `MXFP4_MOE`/`ROCMFP4`, no other scheme field touched); dropped the unused `Dict` typing import from `converters.py` (`Optional` stays, used by `encode_to_ggml_bytes`'s `imatrix`/`n_per_row` params); deleted 5 unreachable rows (`Q4_0`/`Q4_1`/`Q5_0`/`Q5_1`/`IQ4_XS`) from `qat/_ggml_ref.py`'s `_DEQUANT_SYMBOL` table, leaving only the 11 types `SCHEME_FAKE_QUANT` has kernels for. Also swapped `converters.py`'s per-tensor `flat = weights.astype(np.float32).flatten()` to `.ravel()` — halves the redundant-copy overhead on the already-float32/contiguous common path (the `astype` copy is unchanged and remains the sole aliasing guarantee under the writer's parallel encode pool; the imatrix branch still reads shape from the original `weights` array, not `flat`); byte-identity verified for Q8_0/Q4_K_M/Q6_K/MXFP4/BF16/F16 across 2-D C-order, flat, Fortran-order, float64, and strided-view inputs (30/30 identical). (2026-08-08; files: magicquant/quant/schemes.py, magicquant/quant/converters.py, magicquant/qat/_ggml_ref.py; verified: full suite 901 passed/20 skipped unchanged before and after, plus tests/test_quantization_guards.py tests/test_imatrix_encode.py tests/test_block_tables.py tests/test_ggml_binding_cross_check.py tests/test_ggml_decode.py tests/test_schemes_docstring.py tests/test_iq_schemes.py tests/test_ggml_facts_snapshot.py 77 passed, plus a standalone byte-identity matrix check)
- Dead-code + duplication cleanup in `config.py` and the `reselect_tiers.py` tool: deleted `MagicQuantSettings.output_path` (never read anywhere; the sibling `source_path` property stays — `validate_paths()` depends on it); deleted `utils/naming.py`'s zero-caller `generate_config_for_quant`, `get_scheme_bits`, and the `if __name__ == "__main__":` demo block, plus the now-orphaned `get_scheme_by_name` import (`GROUP_CODES`/`get_group_names` and `generate_name` are kept — a later unit repurposes the group-code table, and `generate_name` produces every output GGUF filename); deleted `reselect_tiers.py::_bpw`'s unreachable `if scheme is None: raise KeyError(...)` guard (`get_scheme_by_name` never returns `None`, only raises `ValueError`; the `except (BaselineFitError, KeyError)` tuple in `analyze()` is untouched — it stays load-bearing for a real `KeyError` from `recover_baseline_gb` on heterogeneous group sets); and hoisted `reselect_tiers.py`'s triple `min(better, key=lambda r: r["loss"])` recompute (three redundant O(n) scans building `dominated_by`/`dominated_by_size_gb`/`dominated_by_loss`) into a single `winner` computed once, read via `winner["key"] if winner else None` so `dominated_by` stays `None` (falsy) when no dominator exists, matching `print_report`'s truthiness branch. (2026-08-08; files: magicquant/config.py, magicquant/utils/naming.py, tools/reselect_tiers.py; verified: full suite 901 passed/20 skipped unchanged before and after, plus tests/test_reselect_tiers.py tests/test_config_routing.py 28 passed)

### Fixed
- `search` subcommand's own argparse defaults (`--output-dir` literal `"./output"`, `--target-quant` literal `"MXFP4_MOE"` — both non-`None`) always won inside `_settings_from_args`'s `_maybe()` override check, so `MAGICQUANT_OUTPUT_DIR`/`MAGICQUANT_TARGET_BASE_QUANT` could never reach `MagicQuantSettings` for `search`, contradicting README.md's documented behavior and the already-established `default=None` pattern on the sibling `generate_parser`/`hybrid_parser` (added for exactly this reason). The same two settings feed `search --dry-run`'s "Configuration summary", so the dry run was silently misreporting the resolved output dir / base quant too; and because `generate` *does* honor the env var, a user with `MAGICQUANT_OUTPUT_DIR` set got a real split-brain — `search` wrote `search_results.json` under `./output` while `generate` looked for it under the env dir and failed with "Search results not found". Changed both `search_parser` defaults to `None` (matching `generate_parser`/`hybrid_parser`) and updated their `--help` text to the sibling parsers' wording (`"Output directory (default: MAGICQUANT_OUTPUT_DIR or ./output)"` / `"Target base quantization (default: MAGICQUANT_TARGET_BASE_QUANT or MXFP4_MOE)"`); hoisted and reworded the "Defaults are None..." comment to sit above `--output-dir` so it scopes all four `search` CLI flags instead of appearing to explain only `--generations`/`--population` below it (the framing that made the bug easy to miss on inspection). **Behavior delta**: anyone with `MAGICQUANT_OUTPUT_DIR` and/or `MAGICQUANT_TARGET_BASE_QUANT` exported will now see `search` (and `search --dry-run`) honor them instead of silently discarding them — this can move where multi-GB output GGUFs land, onto a different filesystem for some users. Users without those env vars see byte-identical behavior: `MagicQuantSettings`' own field defaults (`magicquant/config.py:23` `output_dir: str = "./output"`, `magicquant/config.py:26` `target_base_quant: str = "MXFP4_MOE"`) are the same literals the removed argparse defaults used, so the no-env resolved values are unchanged. Explicit `--output-dir`/`--target-quant` CLI flags are unaffected — they still win over env, as before. (2026-08-08; files: magicquant/__main__.py, tests/test_config_routing.py; verified: full suite 928 passed/20 skipped [927 baseline + 1 new test, `test_search_real_parser_env_and_flag_output_dir_target_quant`]; the new test is the first in `tests/test_config_routing.py` to build the real argparse parser via `cli.main()` rather than the file's existing `_args()`/`_search_args()` hand-built-`Namespace` helpers, which default `output_dir`/`target_quant` to `None` already and so could not have caught this class of bug by construction; confirmed the new test FAILS against the pre-fix code — `output_dir` resolved to the literal `"./output"` instead of the env-set tmp dir — before confirming it passes against the fix; `tests/test_config_routing.py tests/test_safe_resolve_corpus.py` 23 passed)
- `_write_pareto_report`'s two consumers of `self._measured` — the `pareto_frontier(measured)` call that builds `pareto.json` and the `log.info(format_pareto_report(measured))` call that logs the human-readable table — passed the raw dict straight through with no `measurement_invalid` filter, unlike the other two consumers of the same dict (`_select_final_survivors`, `_write_noise_calibration`), which both already exclude it. A `measurement_invalid` entry's ppl is below `baseline*(1-eps)` by construction (a physically impossible reading), so on a mixed valid/invalid run it has a lower ppl than every real candidate and dominates the Pareto frontier — silently corrupting both the persisted `pareto.json` and the logged table even though tier selection correctly excluded it. Filtered `self._measured` to drop `measurement_invalid` entries before both calls, logging the excluded count when non-zero (mirroring `tools/reselect_tiers.py`'s "discarded as broken measurements" line) so the "dominated N of M" denominator doesn't silently shrink unremarked. **Behavior delta**: invalid measurements no longer appear in `pareto.json` or the logged Pareto table; runs with no invalid measurements are byte-identical. Caller-side fix only — `magicquant.pareto.load_and_report()` reading a persisted `search_results.json` off disk still shows invalid entries, since `_save_results` deliberately keeps them there for diagnostics. (2026-08-08; files: magicquant/orchestrator.py, tests/test_measurement_validity.py; verified: full suite 929 passed/20 skipped [928 baseline + 1 new test, `test_write_pareto_report_excludes_measurement_invalid_entries`]; targeted `tests/test_orchestrator_measurement.py tests/test_pareto.py tests/test_measurement_validity.py tests/test_probe_resolution.py` 101 passed)
- `predictor_rank_correlation`/`predictor_is_tracking` (`magicquant/utils/measurement.py`) — the Kendall-tau predicted-vs-measured ranking diagnostic the module's own docstring calls "the guard that would have caught the 2026-07 [Laguna-S] failure no matter which layer was at fault" — was fully built and unit-tested but never called by `run_measured_search`, the search it was designed to protect: a single commit (`ff09b77`) wired every other guard from that fix into the orchestrator and missed this one. Wired it in **end-of-run**, not per-round (`candidates_per_round` defaults to 4, far below `MIN_TAU_SAMPLES=12`, so a per-round call would report "unknown" forever and never actually check anything), via a new `_log_predictor_tracking()` called once after the measurement loop's zero-valid-measurements guard: computes tau/is_tracking cumulatively over every accumulated `(predicted_loss, measured_loss)` pair in `self._measured`, excluding `measurement_invalid` entries (a physically impossible reading would corrupt the correlation the same way it corrupts tier selection) and incumbent-seeded entries (`measured_loss=None`, never actually measured). Logs the verdict at INFO when tracking or unknown, WARNING only when NOT tracking — the function returns a three-state True/False/None verdict and only False is evidence of a broken run; logging a None ("not enough data yet") as "not tracking" would be the reporting-layer equivalent of the "measured nothing reported as measured zero" defect this repo's audits keep flagging. Never raises and never gates the search on a low tau. Persists `{is_tracking, tau, n_pairs}` under a new additive `predictor_tracking` key in `_save_results`' output (`null` when never computed, e.g. `run_full_search`'s prediction-only path, which has no measurements to check) — no existing key touched. scipy stays a `[dev]`-only dependency; its absence (verified against `.venv-qat`, which has no scipy installed) degrades to the ordinary `(None, None)`/"unknown" verdict via `predictor_rank_correlation`'s existing one-time `log.warning`, not a crash or per-call warning spam, since this now runs once per search rather than never. (2026-08-08; files: magicquant/orchestrator.py, tests/test_orchestrator_measurement.py; verified: full suite 932 passed/20 skipped [929 baseline + 3 new tests, one `pytest.importorskip("scipy")`-gated]; targeted `tests/test_orchestrator_measurement.py tests/test_pareto.py tests/test_measurement_validity.py tests/test_probe_resolution.py` 104 passed; `.venv-qat/bin/python -m pytest tests/test_probe_resolution.py tests/test_orchestrator_measurement.py -q` → the pre-existing 3 `TestPredictorTracking` failures unchanged [scipy-absent environment gap], new tests: 2 passed + 1 skipped)

## [0.3.0] - 2026-06-09

*Backfills everything shipped since the 0.2.0 entry below. `pyproject.toml`'s version jumped straight from 0.1.0 to 0.3.0 in the commit dated here; no further version bump has landed since, so this single entry also covers work that kept shipping under 0.3.0 through 2026-08-07, the day before the cleanup branch started.*

### Added
- **Sub-4-bit IQ-quant family** (PR3): `IQ4_XS`/`IQ3_S`/`IQ3_XXS`/`IQ2_S`/`IQ2_XS`/`IQ2_XXS`/`IQ1_M`/`IQ1_S` added to the scheme registry, opt-in via `enable_iq` so the default search and its seed-42 regression fixture are unaffected; per-scheme `requires_imatrix` is read from the real libggml rather than guessed.
- **imatrix support** (PR4): `magicquant imatrix` captures an importance matrix via `llama-imatrix`; `create_hybrid_gguf(..., imatrix=...)` threads per-tensor importance vectors (true row width) into the encoder; Pass 1 hard-errors when a target type requires an imatrix but none was given; calibration and evaluation corpora are asserted disjoint.
- **v2 budget search** (`--algo v2`, `docs/redesign.md`): per-tensor × per-scheme distortion table (imatrix-weighted encode+decode through libggml), optional group-probe κ fitting, and an exact multiple-choice-knapsack allocator (`v2/allocate.py`) emitting the full predicted quality-size frontier in one solve; `--target-profile q4nx` restricts choices to NPU-packable schemes.
- **QAT-LoRA** (`magicquant qat`, `docs/qat.md`): fake-quant + straight-through-estimator training (`qat/fake_quant.py`) covering BF16/Q8_0/MXFP4/Q4_K/Q5_K/Q6_K/Q3_K/Q2_K/IQ4_NL, `QATLinear` expert-aware wrapping with `live`/`frozen` expert-quant modes, and a merge pipeline (`merge_qat_adapters`) back to a byte-identical GGUF pack.
- Validated end-to-end: QAT recovers 38.1% of quantization loss in the real shipped GGUF pack (B 12.56 / Q 14.21 / QT 11.94 / BT 10.92 PPL); `frozen` mode recovers roughly 55% of `live`'s recovery at about half the compute.
- **ROCmFPX fork schemes**: AMD-native `ROCMFP3/4/6/8` (ggml ids 100-104) registered opt-in via `enable_rocmfpx`; `ggml_binding` probes fork support by type name so a stock libggml raises a clear error instead of silently misencoding.

### Changed
- **libggml binding rewrite**: deleted ~850 lines of pure-Python K-quant encoders; every quantized type now routes through a ctypes binding calling `ggml_quantize_chunk` — the same function `llama-quantize` uses — proven byte-identical by a new encoder-parity test suite (`tests/integration/test_encoder_parity.py`).
- **Measured-search hardening**: added checkpoint/resume for long measured-search runs; `SensitivityProber(strict=True)` now raises `ProbeMeasurementError` on a failed probe instead of silently poisoning the run with heuristic sensitivities; probes measure against Q8_0 rather than BF16 and are scored by KL divergence against saved reference logits rather than raw perplexity.
- **KL probe/objective split**: `probe_kl` (default on) now gates KL-scored sensitivity probing independently of `enable_kl`, which alone controls the measured-search objective blend; falls back to PPL probing when logit capture isn't possible.

## [0.2.0] - 2026-04-03 — Production Hardening

### Bug Fixes
- **CRITICAL**: Added dtype validation guard in `encode_to_ggml_bytes()` — rejects non-floating-point tensors with descriptive error instead of silently producing corrupt output
- **CRITICAL**: Added dtype guard in GGUF writer worker thread before encoding dispatch
- Fixed `general.file_type` metadata mapping — was hardcoded to `1` for all branches, now uses a proper lookup table mapping quantization schemes to llama_ftype enum values

### Added
- `magicquant/config.py` — Pydantic-settings `BaseSettings` (`MagicQuantSettings`) for configuration via environment variables (`MAGICQUANT_` prefix) and `.env` files. Includes `validate_paths()` for startup checks and typed properties for `output_path` / `source_path`.
- `magicquant/logging.py` — Structured logging via structlog with `configure_logging()` (console or JSON output) and `get_logger()` factory. Consistent schema: `event`, `stage`, `tensor_name`, `progress`, etc.
- Tenacity retry wrappers on `calculate_perplexity()` subprocess call — `@retry(stop=stop_after_attempt(3), wait=wait_exponential(min=1, max=10))` handles transient GPU OOM and file-lock failures.
- `tests/test_quantization_guards.py` — 18 tests covering dtype validation, encoder output sizes, and BF16 round-trip correctness.
- `--dry-run` flag on `magicquant search` command — validates configuration, opens source model, verifies tensor count and architecture, checks llama.cpp availability, and reports a summary without running the search.

### Changed
- **Structured logging in orchestrator**: All `print()` statements in `orchestrator.py` replaced with `structlog` logger calls (`log.info`, `log.warning`, `log.error`). Every event includes a `stage` field for filtering (init, baseline, probing, measurement, build, generate, results).
- **Subprocess hardening** (`llamacpp.py`):
  - All `subprocess.run()` calls have explicit `timeout` parameters (30s for `which`/`where`, 600s for perplexity, 1800s for quantization).
  - No `shell=True` usage (verified by audit).
  - Proper error handling for both `CalledProcessError` and `TimeoutExpired` on every call.
  - Extracted `_parse_perplexity_output()` as a standalone function for testability.
- **Pathlib conversion**: Replaced `os.path.join()`, `os.path.exists()`, `os.path.isfile()`, `os.path.getsize()`, `os.makedirs()`, and `os.remove()` with `pathlib.Path` operations in `orchestrator.py`, `writer.py`, `llamacpp.py`, and `__main__.py`. Public APIs still accept `str` paths.
- **Dependencies**: Added `pydantic-settings>=2.0.0`, `structlog>=24.0.0`, `tenacity>=8.2.0`, `python-dotenv>=1.0.0` to core deps. Added `pytest-asyncio>=0.21.0` to dev deps.

## Future work

Surfaced by the 2026-08 cleanup audit but out of scope for a behavior-preserving pass — each needs its own design decision or investigation before anything touches it:

- Atomic-publish (temp-file + `os.replace`) crash-safety is implemented separately 7 times across 4 subsystems, 3-way diverged — needs one shared implementation.
- Two diverged dequant-symbol tables exist in the codebase and need reconciling into one.
- Safetensors shard discovery is implemented twice and needs folding into one.
- "Load `search_results.json` tier" logic is duplicated across 3 call sites and needs unifying.
- Config-signature computation is duplicated across 6 sites and needs unifying.
- The `tests/` tree itself (19.3k LOC) was never audited for dead code or duplication in this pass.
- `run_full_search` constructs its `SensitivityProber` WITHOUT `parameter_counts` — a real behavioral divergence between the measured-search and prediction-only paths (possible bug, needs investigation).
- A latent interaction between v2's `--target-profile q4nx` and `probe_scheme` was found during verification (possible bug, needs investigation).
- v2's scheme filter is missing v1's `IMATRIX_DEPENDENT_SCHEME_NAMES` gate, which compounds with an imatrix probe-cache issue fixed elsewhere in this cleanup pass.
- `SensitivityProber`'s `baseline_ppl_err` field is structurally unreachable.
- A handful of additional lower-priority items were discussed during the audit but not individually written up (see `docs/cleanup-2026-08-plan.md` for the full notes).
