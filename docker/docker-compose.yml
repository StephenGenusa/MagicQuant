services:
  magicquant:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - ../models:/app/models:ro
      - ../output:/app/output
    environment:
      - PYTHONUNBUFFERED=1
    deploy:
      resources:
        reservations:
          memory: 16G
